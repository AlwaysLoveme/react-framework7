import React from 'react';

import { Page, Navbar } from 'framework7-react';

const Contract: React.FC = () => {
	return (
		<Page>
			<Navbar title="React 仿微信" />
		</Page>
	);
};

export default Contract;
